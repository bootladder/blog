Build the blog locally and check for errors.
jekyll build

Push to github which webhooks to Travis
Travis builds the blog, by zipping and sending to freejekyllbuilder.com

Travis sends a request to https://ccram.org:5000/bootladder-blog

(need to create a repo for the deployer server
Check the deployer server on the same machine.
There's a script in there that finds the latest
github release, downloads it, unzips, copies into
a webserver root directory.



THERE IS NO MORE DOCKER IN THIS REPO.
This repo does not involve building or running a webserver.
All it does is generate the static site,
which can be deployed into a running webserver somewhere else
